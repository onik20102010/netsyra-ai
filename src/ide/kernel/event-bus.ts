// Re-export the production event bus implementation.
export { EventBus, createEvent, Subscription } from "@/ide/events";
export type { EventBusOptions, EventBusMetrics, SubscriptionOptions } from "@/ide/events";

